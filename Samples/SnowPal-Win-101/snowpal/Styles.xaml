<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Define a base style for Title TextBlock -->
    <Style x:Key="BaseTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="Foreground" Value="Black"/>
        <Setter Property="FontSize" Value="48"/>
        <Setter Property="FontWeight" Value="Bold"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="0,50,0,0"/>
    </Style>
 
    <!-- Define a style for the Title TextBlock that inherits from BaseTextBlockStyle -->
    <Style x:Key="MenuTitleTextBlockStyle" TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}">
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Define a style for the Menu Buttons -->
    <Style x:Key="MenuButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Height" Value="50"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <!-- Define a style for the Guesses Left TextBlock -->
    <Style x:Key="GuessesLeftTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="20"/>
    </Style>

    <!-- Define a style for Alphabet Buttons -->
    <Style x:Key="AlphabetButtonStyle" TargetType="Button">
        <Setter Property="Width" Value="48"/>
        <Setter Property="Height" Value="48"/>
        <Setter Property="Margin" Value="2"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="25"/>
    </Style>

    <!-- Define a style for the Word Display TextBlock -->
    <Style x:Key="WordDisplayTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="50"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Bottom"/>
        <Setter Property="Margin" Value="10"/>
    </Style>
    
    <!-- Define a style for the Menu Buttons -->
    <Style x:Key="MenuButtons" TargetType="Button">
        <Setter Property="Width" Value="200"/>
        <Setter Property="Height" Value="50"/>
    </Style>


    <!-- Define a style for the Popup -->
    <Style x:Key="PopupStyle" TargetType="Popup">
        <Setter Property="VerticalOffset" Value="300"/>
        <Setter Property="HorizontalOffset" Value="800"/>
    </Style>
    
    <!-- Define a style for the MessageTitle Popup TextBlockStyle -->
    <Style x:Key="MessageTitlePopupTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="50"/>
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>
 

    <!-- Define a style for MessageContent Popup TextBlockStyle -->
    <Style x:Key="MessageContentPopupTextBlockStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="24"/>
        <Setter Property="TextWrapping" Value="Wrap"/>
    </Style>

    <!-- Define a style for the Popup Button -->
    <Style x:Key="PopupButtonStyle" TargetType="Button">
        <Setter Property="HorizontalAlignment" Value="Right"/>
    </Style>

    <!-- Define a base Center style for StackPanel  -->
    <Style x:Key="HorizontalCenterStackPanel" TargetType="StackPanel">
        <Setter Property="HorizontalAlignment" Value="Center"/>
    </Style>

    <Style x:Key="TopHorizontalCenterStackPanel" TargetType="StackPanel" BasedOn="{StaticResource HorizontalCenterStackPanel}">
        <Setter Property="Orientation" Value="Horizontal"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="10"/>
    </Style>

    <Style x:Key="CenterHorizontalCenterStackPanel" TargetType="StackPanel" BasedOn="{StaticResource HorizontalCenterStackPanel}">
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Spacing" Value="12"/>
    </Style>
    
    <!-- Define style for Image -->
    <Style x:Key="ImageStyle" TargetType="Image">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
        <Setter Property="Margin" Value="10,10,10,10"/>
    </Style>

    <!-- Define style for GridView -->
    <Style x:Key="GridViewStyle" TargetType="GridView">
        <Setter Property="HorizontalAlignment" Value="Center"/>
        <Setter Property="VerticalAlignment" Value="Center"/>
        <Setter Property="Margin" Value="10,50,10,10"/>
    </Style>

    <!-- Define style for GridView -->
    <Style x:Key="GridStyle" TargetType="Grid">
        <Setter Property="HorizontalAlignment" Value="Left"/>
        <Setter Property="VerticalAlignment" Value="Top"/>
    </Style>

    <!-- Define style for Border -->
    <Style x:Key="BorderStyle" TargetType="Border">
        <Setter Property="Padding" Value="20"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Width" Value="500"/>
        <Setter Property="Height" Value="400"/>
        <Setter Property="BorderThickness" Value="2"/>
    </Style>


</ResourceDictionary>
